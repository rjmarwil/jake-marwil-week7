<%= form_for(@user) do |f| %>

<% if @user.errors.any? %>
  <h2><%= pluralize(@user.errors.count, "error") %> prohibited this <%= @user.class %> from being saved:</h2>
  <ul>
  <% @user.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
  </ul>
<% end %>

<br/>

<%= f.label :user_name %>
<%= f.text_field :user_name %>

<br/>

<%= f.label :about %>
<%= f.text_field :about %>

<br/>

<%= f.label :password %>
<%= f.password_field :password %>

<br/>

<%= f.label :password_confirmation %>
<%= f.password_field :password_confirmation %>

<br/>

<%= f.submit %>
<a href="/users">Cancel</a>

<% end %>
